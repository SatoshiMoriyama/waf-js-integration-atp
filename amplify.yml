version: 1
applications:
  - appRoot: packages/login-front
    frontend:
      phases:
        build:
          commands:
            - echo "no build step needed (static html)"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
      cache:
        paths: []
